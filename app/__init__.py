"""Bulk processing system package.

This package implements a small FastAPI application for bulk hospital
processing. Modules under this package include CSV parsing utilities,
downstream client integration, batch processing state, and the HTTP
application entrypoints.

The docstrings across modules follow the NumPy style for consistency.
"""
